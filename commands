#/bin/bash
case "$1" in
	git-build-locked)
		APP="$2"
		REF="$3"
	 	branch=`cat HEAD | cut -d '/' -f3`
		commit=`cat refs/heads/$branch | cut -d '/' -f3`
		dokku config:set-norestart $APP _GIT_BRANCH=$branch _GIT_COMMIT=$commit
		
		;;
esac 
